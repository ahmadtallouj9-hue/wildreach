/** Local voxel workspace for custom models (swords, creatures, characters). */
export const LOCAL_GRID_SIZE = 32;

/** Total cells in the editor volume. */
export const LOCAL_GRID_VOLUME = LOCAL_GRID_SIZE ** 3;
